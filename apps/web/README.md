# apps/web

Frontend Next.js. Non implementato in Fase 8.1a.

Verrà aggiunto in **Fase 8.1b** con:
- home con banner "MVP-0 / MockProvider";
- pagina `/diagnostic` che chiama `/api/proxy-health` (BFF) → `GET /health/ready` dell'API.